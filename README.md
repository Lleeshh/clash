# Clash Royal Clan Info
Simple script to fetch Clash Royal (TM) info via the RoyalApi (TM).

## Setup
1. pip install requests
1. Get a RoayalApi Dev key
1. Get your clan tag
1. Add a command line parmaeter such as -c <clan tag>
1. Run and an output files in .csv format are output
